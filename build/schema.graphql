type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  owner: Bytes! # address
  approved: Bytes! # address
}

type BorrowerRecord @entity {
  id: ID!
  contract: Bytes!
  nftId: BigInt!
  lender: Bytes!
  renter: Bytes!
}